# =============================================================================
# SMILE Experiment Configuration — Safe Defaults (tracked by git)
# =============================================================================
# These are non-secret defaults for your experiment.
# Override locally in .env.local (gitignored) or in design.js via
# api.setRuntimeConfig().
#
# For secrets (database tokens, passwords), see .env.example.
# =============================================================================

# --- UI & Display ---
VITE_COLOR_MODE                  = light
VITE_RESPONSIVE_UI               = true
VITE_WINDOWSIZER_REQUEST         = 800x600
VITE_WINDOWSIZER_AGGRESSIVE      = true
VITE_ANONYMOUS_MODE              = false

# --- Branding ---
VITE_LAB_URL                     = https://mylab.edu
VITE_BRAND_LOGO_FN               = universitylogo.png

# --- Experiment behavior ---
VITE_ALLOW_REPEATS               = false
VITE_RANDOM_SEED                 = 100012

# --- Data saving ---
VITE_AUTO_SAVE_DATA              = true
VITE_MAX_WRITES                  = 1000
VITE_MIN_WRITE_INTERVAL          = 2000

# --- Stepper ---
VITE_MAX_STEPS                   = 5000

# --- Dev/presentation mode ---
# SMILE_DEV_PASSWORD is intentionally NOT set here — it's a secret.
# Local dev: not needed (middleware skips the gate when import.meta.dev).
# Production: set it in your Vercel dashboard (Settings > Environment Variables).
# To put a value in for local testing of the login flow, put it in .env.local.
# Set to true to allow /presentation without a password
SMILE_PUBLIC_PRESENTATION        = false
