# Project Notes

## Tooling
- This project uses **pnpm**, not npm. Use `pnpm` for all commands (e.g., `pnpm lint`, `pnpm test`, `pnpm dev`).
- Use `pnpm exec` instead of `npx` to avoid npm warnings about pnpm-specific `.npmrc` settings.
